
<% @pages.each do |page| %>
    <div class="col-sm-6 col-md-3">
      <div class="thumbnail">
      	<%= page.title %>
      	<%= page.user.email %>
        <%= link_to (image_tag page.image.url(:medium), class: 'image'), page %>
      </div>
    </div>
  <% end %>